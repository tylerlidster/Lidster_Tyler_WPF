// Expressions with Arrays

var orangeBins = [234, 567, 883];

//Identify each bin number in the array
//Write the name of the array [] then the number in the array we want to access
var total = orangeBins[0] + orangeBins[1] + orangeBins[2];
console.log(total);

//Machine adds all items in the array and gives the total